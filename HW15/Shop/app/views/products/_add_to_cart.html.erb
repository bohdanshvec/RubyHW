  <% if current_user %>
  
    <%= turbo_frame_tag dom_id(product) do %>
      <%= button_to 'Add to cart', line_items_path(product_id: product.id), class: 'btn btn-outline-light me-2 mb-3 mt-3' %>
    <% end %>

  <% end %>
