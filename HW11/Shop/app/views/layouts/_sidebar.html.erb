<!--<div class="col-2 flex-shrink-0 p-3 text-white bg-dark hhh">-->
<div class="col-2 flex-shrink-0 p-3 text-white hhh" style="background-color: #e3f2fd;">

  <div class="category">
    <h4>Categories:</h4>
  </div>
  <ul class="nav nav-pills flex-column mb-auto">
      <% Category.all.each do |category| %>
        <li class="nav-item">
          <!--<%= link_to category.title, category %>-->
          <%= link_to category.title, products_path(category) %>
        </li>
      <% end %>
    </ul>
</div>